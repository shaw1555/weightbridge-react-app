export interface Setup {
  setup_id_f: number;
  category_f: string;
  description_f: string;
  note_f: string;
  option1_f: string;
  is_default_f: boolean;
  log_by_f: string;
  log_date_time_f: string; // DateTime represented as ISO string in JSON
}
